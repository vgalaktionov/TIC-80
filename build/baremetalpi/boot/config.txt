# Circle renders to display 0 (the HDMI port nearest USB-C). Ignore the KVM's
# EDID contents but preserve physical hotplug events for runtime link retraining.
hdmi_ignore_edid:0=0xa5000080
hdmi_force_edid_audio:0=1
hdmi_drive:0=2
hdmi_group:0=1
hdmi_mode:0=16
max_framebuffers=1

scaling_kernel=8
